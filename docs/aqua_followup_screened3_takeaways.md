# AQuA Follow-Up Screened-3 Takeaways

- `n=3` only.
- Screening 200 candidates found only 3 clean partial-correct items.
- Final majority accuracy was `1/3`.
- All-round majority and oracle-history style checks were `2/3`.
- `aqua_test_1_000086` is a `transient_correct_consensus_lost` case: a correct unanimous consensus appears in an intermediate round, then the final round loses it.
- This is an additional diagnostic example, not a replication claim.
- No statistical claims should be drawn from `n=3`.
